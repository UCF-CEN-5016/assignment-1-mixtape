// Adds the jest-dom matchers (toBeInTheDocument, toHaveTextContent, ...) to
// Vitest's expect. Loaded automatically via vite.config.js -> test.setupFiles.
import "@testing-library/jest-dom/vitest";
